# v2.0.1 リリース準備 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Issue #428 の bugfix を含む v2.0.1 patch release のリリース準備（バージョンバンプ・CHANGELOG・README・CSS コメント更新）を行う。

**Architecture:** `develop` から `release/2.0.1` ブランチを作成し、3 コミット（バージョンバンプ / CHANGELOG / ドキュメント更新）を順に積む。実装コードの変更は含まない。

**Tech Stack:** Markdown, JSON, CSS (ドキュメントのみ)

**Branch:** `develop` から `release/2.0.1` を作成して作業する。

---

## ファイル構成

| 操作 | ファイル | 責務 |
|------|---------|------|
| Modify | `package.json` | バージョン `2.0.0` → `2.0.1` |
| Modify | `CHANGELOG.md` | 2.0.1 エントリ追加 |
| Modify | `README.md` | Page Break セクション + Release Notes 更新 |
| Modify | `README.ja.md` | 改ページセクション + Release Notes 更新 |
| Modify | `styles/markdown-pdf.css` | Page Break コメント更新 |

---

### Task 1: release ブランチ作成

**Files:**
- なし (git 操作のみ)

- [ ] **Step 1: release ブランチを作成してチェックアウト**

```bash
git checkout develop
git checkout -b release/2.0.1
```

---

### Task 2: バージョンバンプ

**Files:**
- Modify: `package.json`

- [ ] **Step 1: package.json のバージョンを更新**

`package.json` の `"version"` フィールドを変更する:

```json
"version": "2.0.1",
```

- [ ] **Step 2: コミット**

```bash
git add package.json
git commit -m "chore: bump version to 2.0.1"
```

---

### Task 3: CHANGELOG 更新

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 2.0.1 エントリを追加**

`CHANGELOG.md` の `## 2.0.0 (2026/04/13)` の直前に以下を挿入する:

```markdown
## 2.0.1 (2026/04/14)

### Fixes

* Fix: Self-closing `<div class="page" />` now correctly triggers a page break [#428](https://github.com/yzane/vscode-markdown-pdf/issues/428)

```

- [ ] **Step 2: コミット**

```bash
git add CHANGELOG.md
git commit -m "docs: add 2.0.1 changelog entry"
```

---

### Task 4: ドキュメント・CSS コメント更新

**Files:**
- Modify: `README.md:635-641`
- Modify: `README.md:731-741`
- Modify: `README.ja.md:632-638`
- Modify: `README.ja.md:738-748`
- Modify: `styles/markdown-pdf.css:44`

- [ ] **Step 1: README.md の Page Break セクションを更新**

635-641行目を以下に変更する:

```markdown
### Page Break

Please use either of the following to insert a page break.

``` html
<div class="page"/>
```

``` html
<div class="page"></div>
```
```

- [ ] **Step 2: README.md の Release Notes セクションに 2.0.1 を追加**

`### 2.0.0 (2026/04/13)` の直前（733行目付近）に以下を挿入する:

```markdown
### 2.0.1 (2026/04/14)
* Fix: Self-closing `<div class="page" />` now correctly triggers a page break [#428](https://github.com/yzane/vscode-markdown-pdf/issues/428)

```

- [ ] **Step 3: README.ja.md の改ページセクションを更新**

632-638行目を以下に変更する:

```markdown
### 改ページ

改ページを挿入するには、以下のいずれかを使用してください。

``` html
<div class="page"/>
```

``` html
<div class="page"></div>
```
```

- [ ] **Step 4: README.ja.md の Release Notes セクションに 2.0.1 を追加**

`### 2.0.0 (2026/04/13)` の直前（740行目付近）に以下を挿入する:

```markdown
### 2.0.1 (2026/04/14)
* Fix: 自己閉じタグ `<div class="page" />` で改ページが正しく動作するよう修正 [#428](https://github.com/yzane/vscode-markdown-pdf/issues/428)

```

- [ ] **Step 5: styles/markdown-pdf.css の Page Break コメントを更新**

44行目を以下に変更する:

```css
/* Page Break : use <div class="page"/> or <div class="page"></div> to insert page break
```

- [ ] **Step 6: コミット**

```bash
git add README.md README.ja.md styles/markdown-pdf.css
git commit -m "docs: update page break docs for both tag forms"
```
